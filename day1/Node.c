#include "Node.h"

struct Node* find(int value, struct Node* L){
    if(L == NULL){

    }
}

struct Node* add2back(int value, int counter, struct Node* L){
    if(L == 0){ //identifier NULL is undefined error, use 0 instead
        return struct Node* newNode{value, 1, NULL};
    }
}